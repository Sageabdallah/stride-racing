requests
beautifulsoup4
# ML stack pinned to the venv that trains and unpickles the artifacts —
# drift here is how a model pkl stops loading, and unpinned scikit-learn
# resolved to 1.9.0 (no cp311 wheels) and broke the image build outright.
pandas==3.0.3
numpy==1.26.4
scikit-learn==1.8.0
scipy==1.17.1
catboost==1.2.10
lightgbm==4.6.0
xgboost==3.2.0
shap==0.49.1
imbalanced-learn==0.14.2
category_encoders==2.10.0
feature-engine==1.9.4
optuna==4.9.0
hyperopt==0.3.0
# pytorch-tabnet (and its ~2.5GB torch dependency) removed 2026-08-02:
# nothing in the repo imports tabnet or torch, and neither model pickle
# references them (the ensemble is CatBoost/LightGBM/XGBoost/sklearn).
# Every Fargate task pulls this image on cold start, so the weight was
# pure startup latency. Re-add here if a tabnet model is ever trained.
tabulate
colorama
psycopg2-binary
# retrain_preflight.py runs the two parity suites in-process (gate 5 of
# docs/project_retrain_gate.md), and the stride-preflight Fargate task runs
# it every morning through gate_status.py. Without pytest in the image the
# import inside gate_parity_suites raised and gate 5 could only ever read
# "unreadable preflight output" where it is scheduled — the gate the
# 2026-09-05 repair was meant to make measurable. Unpinned like the other
# tooling; it is a test runner, not part of the artifact's unpickle path.
pytest
playwright
dnfile
networkx==3.6.1
python-louvain
groq
sqlalchemy==2.0.51
python-dateutil
python-dotenv
tavily-python
anthropic
